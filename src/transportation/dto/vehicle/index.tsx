export interface VehicleDto {
  name: string;
  user: string;
  plate: string;
  type: string;
  phone: string;
  is_active: boolean;
}

export interface VehicleRestDto {
  id: number;
  name: string;
  user: string;
  plate: string;
  type: string;
  phone: string;
  is_active: boolean;
}
